import { CheckSquare } from 'lucide-react';

export function Logo() {
  return (
    <div className="flex items-center justify-center size-12 bg-primary/10 rounded-lg shadow-sm">
      <CheckSquare className="size-6 text-primary" />
    </div>
  );
}
